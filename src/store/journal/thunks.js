export const startNewNotes = () => {
    return async( dispatch ) => {
        //uid

        const newNote = {
            title: '',
            body: '',
            date: new Date().getTime(),
        }

        // dispatch
        // dispatch(newNote)
        // dispatch(activarNota)
    }
}